# Core dependencies
pymongo>=4.0.0
numpy>=1.20.0
pandas>=1.3.0
scikit-learn>=1.0.0
flask>=2.0.0
requests>=2.25.0
pytz>=2021.1
geopy>=2.2.0
beautifulsoup4>=4.9.0
lxml>=4.6.0
python-dateutil>=2.8.0

# Optional ML libraries (uncomment if needed)
# Note: These are optional - the code handles their absence gracefully
# XGBoost requires OpenMP runtime on macOS: brew install libomp
xgboost>=1.5.0
lightgbm>=3.3.0
shap>=0.40.0

# CatBoost requires build tools (cmake, ninja) and may not have wheels for Python 3.14
# If you need CatBoost, install build tools first:
#   macOS: brew install cmake ninja
#   Then: pip install catboost>=1.0.0
# Or try installing from a pre-built wheel if available
# catboost>=1.0.0

# LangChain and agent dependencies
langchain>=0.1.0
langchain-openai>=0.0.5
langchain-community>=0.0.20
pydantic>=2.0.0
openai>=1.0.0
toon>=0.1.0

